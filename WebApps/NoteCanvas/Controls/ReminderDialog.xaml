<Window x:Class="NoteCanvas.Controls.ReminderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تعيين تذكير" Height="200" Width="350" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" WindowStyle="ToolWindow" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="اختر وقت التذكير:" FontSize="14" Margin="0,0,0,8"/>
        <DatePicker Grid.Row="1" Name="DatePicker" Margin="0,0,0,8"/>
        <TextBox Grid.Row="2" Name="TimeTextBox" Text="12:00" Margin="0,0,0,12"/>
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="إلغاء" Margin="0,0,4,0" Click="Cancel_Click"/>
            <Button Grid.Column="1" Content="موافق" Margin="4,0,0,0" IsDefault="True" Click="Ok_Click"/>
        </Grid>
    </Grid>
</Window>
